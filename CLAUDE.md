project-workflow v1.0 기준으로 초기화됨 (2026-09-01)

# 이 프로젝트에서 지킬 것

- 현재 버전 문서는 `docs/current/`에 있다. 5종은 사람이 쓴다. **고치지 않는다**
- `backlog.json`은 CLI로만 바꾼다. 직접 편집하지 않는다
- 진행 중에 task를 추가하지 않는다. 계획 밖 작업은 `docs/current/PROGRESS.md`에 적는다
- task를 닫을 때마다 `PROGRESS.md`에 무엇을·결과·검증을 남긴다
- `docs/history/` 아래는 읽기만 한다. 수정도 삭제도 하지 않는다
- 완료 조건은 `PLAN.md`·`backlog.json`에 적힌 것으로 판정한다. 스스로 정하지 않는다
- 요구가 바뀌면 `SPEC.md`부터 고친다. 코드나 backlog로 우회하지 않는다
- 되돌릴 수 없는 작업(배포·삭제·외부 상태 변경)은 먼저 묻는다

# 이 코드에서 지킬 것

- 껍데기(`shell/`, `shared/design/`)는 템플릿 그대로 쓰고 수정하지 않는다 (v0.1 D-2, NFR-4)
- 빌드 단계를 넣지 않는다. 번들러·트랜스파일러 없이 무빌드로 로드한다 (v0.1 D-5, 제약 5)
- 리소스 지식과 확장자 판정은 `presets/markdown/` 안에만 둔다 (v0.1 D-4)
- 렌더 결과는 화면 삽입 전 살균(sanitize)하고 파일 접근은 브릿지를 거친다 (v0.1 D-6)
- 텍스트·코드 파일은 2MB 상한, 바이너리 널 바이트, UTF-8 외 인코딩을 거절한다 (v0.1 D-8)
